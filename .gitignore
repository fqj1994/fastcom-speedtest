# Compiled binary — rebuild with: go build -o fastcom-speedtest .
/fastcom-speedtest
/fastcom-speedtest.exe

# Local measurement output. JSON/human reports embed the client IP, ISP, ASN and
# target edge locations, so they are deliberately kept out of the repository.
/results/

# Go test / coverage artifacts
*.test
*.out
coverage.txt

# Editor and OS noise
*.swp
*.swo
*~
.DS_Store
